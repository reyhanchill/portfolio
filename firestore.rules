rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public portfolio content only. Administrative publishing uses IAM credentials.
    match /{collection}/{document} {
      allow read: if collection in ['projects', 'skills', 'stats', 'profile'];
      allow write: if false;
    }
  }
}
