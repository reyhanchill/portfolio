{
  "name": "matrix-portfolio",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "firebase": "^12.14.0",
    "framer-motion": "^12.40.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^5.7.0",
    "react-router-dom": "^7.12.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "prestart": "node scripts/metadata.js",
    "prebuild": "node scripts/metadata.js",
    "lint": "eslint src scripts --no-cache",
    "seed": "node scripts/seed.js"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "firebase-admin": "^14.4.0"
  }
}
